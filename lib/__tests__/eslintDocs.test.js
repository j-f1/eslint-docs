const assert = require('assert');
const eslintDocs = require('../index.js');

describe('eslintDocs', function () {
  it('has a test', function () {
    assert(false, 'eslintDocs should have a test');
  });
});
